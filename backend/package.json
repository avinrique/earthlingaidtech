{
  "name": "earthlingaidtech-leads",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Lead capture API + lead management dashboard for earthlingaidtech.com",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "migrate": "node --env-file-if-exists=.env.local scripts/migrate.mjs",
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/nodemailer": "^6.4.17",
    "@vercel/node": "^5.1.0",
    "typescript": "^5.7.2"
  }
}
